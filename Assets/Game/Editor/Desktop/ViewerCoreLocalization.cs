namespace GoLive.Editor.Desktop
{
    // Viewer Core: the LIVE overlay's donation highlight. Viewer chat text itself is never localized content.
    internal static class ViewerCoreLocalization
    {
        internal static readonly string[] Lines =
        {
            "desktop.overlay.donation_amount|${0:0.00}|${0:0.00}"
        };
    }
}
