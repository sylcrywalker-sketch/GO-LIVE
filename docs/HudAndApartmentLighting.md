# HUD и свет квартиры — проход полировки

Игровой HUD по утверждённой композиции (скриншот A) и утренний свет/атмосфера реальной сцены GL. Геймплей, магазин, инвентарь и сборка ПК не менялись.

## 1. HUD (`Assets/Game/Prefab/HUD/[HUD].prefab`)

| Блок | Содержимое | Владелец текста/значений |
| --- | --- | --- |
| `Status` (слева сверху, 396×222) | `Time`: День · время · день недели; строка аренды; разделитель; строки `Hunger` и `Concentration` (иконка, подпись над полосой, значение справа) | `GameTimeHudView`, `RentHudView`, `NeedsHudView` на корне `[HUD]` |
| `Balance` (справа сверху, 224×76) | иконка кошелька, «Баланс», сумма | `WalletHudView` |
| `InventoryUI/InventoryHint` (справа снизу) | клавиша `TAB` (`Key`) + «Инвентарь» | `InventoryUiController.closedHintText`; `Key` — дочерний объект и скрывается вместе с подписью |

- Режим сборки ПК гасит `Status`, `Balance`, `InventoryHint` (`PcWorkbenchHudView.gameplayHud`).
- Спрайты — `Assets/Game/UI/HUD/Sprites` (генерируются `Tools/HudSprites/hud_sprites.py`, метаданные с 9-slice хранить). Материал текста с мягкой подложкой — `Assets/Game/UI/HUD/LiberationSans SDF - HUD.mat`.
- Потребности: значение в процентах, полоса плавно догоняет цель (`_barResponseSeconds`). Предупреждение: голод ≥ 75 %, концентрация ≤ 25 % — полоса/иконка/значение уходят в тёплый красный, один короткий всплеск при входе в состояние, без пульсации. Новая игра стартует с концентрацией 20 % (`DefaultPlayerNeedsConfig`), поэтому строка сразу показана низкой.
- Баланс при изменении досчитывает до новой суммы за доли секунды; размер текста фиксирован, раскладка не прыгает.

## 2. Строка аренды

Правило живёт в одном месте — `RentHudPresentation` (plain C#): `Evaluate(RentRules, RentSnapshot, GameTimeSnapshot)` → статус + сумма + дни, `Localize(...)` → две части строки. `RentHudView` только красит статус и собирает `часть • статус`. Фаза аренды — `RentSnapshot.Phase` (раньше дублировалась во вью).

| Фаза | Текст (RU / EN) |
| --- | --- |
| Первый платёж, до срока N дней | «Аренда: $50.00 • осталось 2 дня / остался 1 день» · «Rent: $50.00 • 2 days left / 1 day left» |
| День срока | «… • сегодня день оплаты» · «… • due today» |
| Просрочка первого платежа | «Аренда: $70.00 • оплата просрочена» · «Rent: $70.00 • overdue» |
| Первый оплачен, второй не выставлен | «Аренда оплачена • следующая через N дней» · «Rent paid • next due in N days» |
| Второй счёт выставлен | как первый платёж (срок — `FinalDeadlineDay`) |
| Всё оплачено / провал | «Аренда оплачена» / «Аренда не оплачена» |

Дней осталось = день срока − текущий день (платёж действует до конца дня срока). Строка обновляется на `Rent.Changed`, `Clock.DayChanged` и смене языка. Числительные: `LocalizationContext.FormatCount(key, n)` берёт `key.one/few/many` по правилу языка (`LocalizationRuntime.PluralForm`). День недели: `GameTimeSnapshot.DayOfWeek`, День 1 — понедельник.

## 3. Свет (сцена GL)

`DayLightingView` (на `[GameSystems]`) — единственный владелец времени суток в свете. Четыре профиля — вид в начале фазы (утро 06:00, день 10:00, вечер 18:00, ночь 22:00), между ними линейная интерполяция; ночь держится тёмной до рассвета (прогресс³).

| Поле профиля | Что делает |
| --- | --- |
| `SunColor`, `SunIntensity`, `SunElevation`, `SunHeading` | солнце/луна (`Lighting/Directional Light`, мягкие тени). Heading — куда летит свет; окно смотрит на +X, утром и до ~12:30 солнце входит в окно над соседним домом |
| `AmbientSky/Equator/Ground` | градиентный ambient (Trilight): пол/стены/потолок |
| `Daylight` | множитель авторских интенсивностей `daylightFills`: `Window Skylight` (спот снаружи окна, без теней), `Window Glow` (свет вокруг окна и штор), `Room Bounce` (мягкое отражённое заполнение к столу), `Courtyard Daylight` |
| `Lamps` | множитель 13 ламп квартиры (`Lighting/Apartment Lamps`), вечером и ночью |

- `Courtyard Daylight` светит только на рендер-слой `Exterior` (стены двора и земля), чтобы кирпич за окном выглядел дневным и не засвечивал комнату.
- Отражения: `Lighting/Room Reflections` — realtime-проба (refresh via scripting, коробочная проекция), перерисовывается при сдвиге игрового времени на 20 минут. Для этого в уровне качества PC включены realtime reflection probes.
- Стекло окна (`M_Window_001`, альфа 0,035) получило alpha clip 0,5: солнце проходит через стекло, рама отбрасывает тень.
- Тени: дистанция 25 м, каскады 0,12/0,30/0,55 (`PC_RPAsset`). SSAO: 0,6 / радиус 0,32 / High (`PC_Renderer`).

## 4. Пост-обработка и атмосфера

- Камера игрока: post-processing включён (раньше был выключен — не было тонмаппинга), SMAA High, dithering.
- `Lighting/Global Volume` → `Assets/Game/Art/Atmosphere/GL Atmosphere Profile.asset`: ACES, экспозиция +1.1, контраст 8, насыщенность −4, тени чуть холоднее / света чуть теплее, bloom 0.16 от порога 1, виньетка 0.2.
- `Atmosphere/Window Sunlit Air` — объём у окна: шейдер `GO LIVE/Atmosphere/Sunlit Air` проходит лучом по коробке и берёт тень солнца из shadow map; плотность 0.035. Сам следует за солнцем и гаснет, когда солнце не светит в окно.
- `Atmosphere/Window Dust` — пылинки у окна (`Sunlit Dust`): видны только в прямом солнце, ярче против света.
- Оба объекта на слое TransparentFX и исключены из пробы отражений.

## 5. Проверка

`RentHudPresentationTests` (29): отсчёт 2 → 1 → «сегодня» → просрочка, смена ровно в полночь, RU/EN строки, оплата и следующий срок, финальный день, провал, формы числительных, ключи каталога, дни недели. Кадры реального Game view 1920×1080 из сцены GL — в отчёте к PR #15.
